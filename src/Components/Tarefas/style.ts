import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    rowView: {
        marginLeft: 20,
        flexDirection:'row',
        justifyContent: 'space-between',
    },
})